(()=>{'use strict';const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const opening=$('#opening'),replay=$('#replayOpening'),key='aise-opening-seen-v3';function play(force=false){if(!opening)return;opening.classList.remove('opening-go','opening-crack-on','is-done');opening.style.display='grid';document.body.style.overflow='hidden';setTimeout(()=>opening.classList.add('opening-crack-on'),220);setTimeout(()=>opening.classList.add('opening-go'),700);setTimeout(()=>{opening.classList.add('is-done');document.body.style.overflow='';if(!force)sessionStorage.setItem(key,'1')},1900)}if(opening){if(sessionStorage.getItem(key)!=='1'&&!matchMedia('(prefers-reduced-motion:reduce)').matches)play();else opening.classList.add('is-done')}replay?.addEventListener('click',()=>play(true));const nav=$('#siteNav');let raf=0;function scroll(){raf=0;nav?.classList.toggle('scrolled',window.scrollY>12)}addEventListener('scroll',()=>{if(!raf)raf=requestAnimationFrame(scroll)},{passive:true});scroll();const items=$$('[data-reveal]');if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion:reduce)').matches){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{rootMargin:'-8% 0px -8% 0px',threshold:.05});items.forEach(x=>io.observe(x))}else items.forEach(x=>x.classList.add('in'));const tb=$('#themeButton');tb?.addEventListener('click',()=>document.documentElement.classList.toggle('light-mode'));$$('.showcase-tabs button').forEach(b=>b.addEventListener('click',()=>{$$('.showcase-tabs button').forEach(x=>x.classList.remove('active'));$$('.preview-panel').forEach(x=>x.classList.remove('is-active'));b.classList.add('active');$(`[data-panel="${b.dataset.preview}"]`)?.classList.add('is-active')}));$('.app-bottom-nav [data-phone-tab]').forEach(b=>b.addEventListener('click',()=>{const name=b.dataset.phoneTab;$('.app-bottom-nav [data-phone-tab]').forEach(x=>x.classList.toggle('active',x===b));$('.phone-preview-panel').forEach(x=>x.classList.toggle('is-active',x.dataset.phonePanel===name));}));$('.nav-button,.footer-cta').forEach(a=>a.setAttribute('href','/app/'));})();
